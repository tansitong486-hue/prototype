本目录为 ColdStation-GlobalOpt-Demo 的 Vite 静态构建产物，供「冷站全局寻优」iframe 嵌入。

重建：
  cd ~/Desktop/ColdStation-GlobalOpt-Demo
  npm run build
  rsync -a --delete dist/ "<产品经理操作系统>/05-原型/AI原型html/cold-station-global-opt-demo/"

勿手改此处 JS/CSS；改源工程后重新 build 覆盖。
