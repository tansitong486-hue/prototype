# AI 节能原型 — Agent 地图

> 原型侧入口。产品价值与域叙事见  
> `07-产品文档/01-ai 节能/产品文档/00-产品全貌.md`。  
> 机读关系：同目录 `product-graph.yaml`。  
> 最后更新：2026-07-31

---

## 1. 双入口约定

| 文件 | 用途 |
|------|------|
| `index.html` → `topologyData` | 站点地图：父子页、版本、简述 |
| `components/nav.js` | 顶栏一级菜单 + 产品侧栏一级菜单 + 版本角标 |

「原型导航」在顶栏全局入口（链到 `index.html`），不在侧栏。子流程页一般只进拓扑、不进侧栏。改页后两者与 `product-graph.yaml` 应同步。

**v2.5 需求方案 HTML**：真源在 `04-需求文档/AI节能v2.5 版本/`；发布与 `index.html` 入口用副本目录 `需求方案/`（含消费侧 iframe 依赖的三份选择器原型）。改真源后需同步复制再 publish。

---

## 2. 命名对照（易踩坑）

| 侧栏文案 | 文件名 | 备注 |
|----------|--------|------|
| 冷站柔性寻优 | `冷站柔性调优.html` | 文件名≠菜单 |
| 能源计费配置 | `能源配置.html` | 文件名≠菜单 |
| 能源拓扑配置 | `能源计量关系.html` | 同时维护能耗计量与供电关系；文件名保留以兼容既有链接 |
| 冷站仿真对比 | `节能仿真对比-设定.html` | |
| 表具能耗明细 | `能耗管理-表具明细.html` | |
| 模型管理（工程配置 → 物模型） | `物模型-模型管理.html` | 现网既有形态、v2.5 无改造无批注；详情页 `物模型-模型详情.html` 只进拓扑不进侧栏，指标 Tab 是本期唯一新增，且只做指标清单展示 |
| AI节能配置（nav） | — | index 模块名写作「AI寻优配置」 |
| 安全回退策略 | `安全回退策略.html` | 已补齐 |

冷站：nav 四页平铺；index 把全局/长周期挂在柔性子树。以 nav 为菜单真源，以 index 为子页真源。

---

## 3. 六条主流程

### 3.1 多工况探索 `flow.multi_condition_explore`

`多工况探索-列表` → `多工况探索-策略制定` → `多工况探索-执行详情` → `多工况探索-验证详情` →（可选）`机理模型训练`

绑定：仿真实例、安全回退；探索范围为内置默认值后可编辑，不绑定安全边界或下发策略。执行仅自动下发，兼容水冷一次/二次泵与风冷冷水系统。验证页复用仿真校验结构，不展示置信度；验证不入库，重训在机理模型训练。

### 3.2 冷站寻优族 `flow.cold_station_opt_family`

页内互跳：`节能仿真对比-设定` ⇄ `冷站柔性调优` ⇄ `冷站全局寻优` ⇄ `冷站长周期寻优`  
仿真对比：`设定` → `节能仿真对比-对比`

运行时绑定（非硬跳转）：仿真模型、安全边界、智控下发、安全回退。

### 3.2.1 冷站智能诊断与规则 `flow.cold_station_diagnosis`

`冷站智能诊断`（结果看板：待处理/全部、筛选、历史弹窗）⇄ 配置侧 `冷站诊断规则`（启用开关 + 规则配置阈值）

对照现网 v1.5 已有能力补齐原型；侧栏分别在「冷站能效优化 / 智能诊断」与「AI节能配置 / 冷站诊断规则」。

### 3.3 能耗配置 → 消费 `flow.energy_config_to_consume`

配置：`物模型-模型管理` → `物模型-模型详情` / `能效视图` / `能源配置` / `计费方案配置` / `能源拓扑配置`（文件：`能源计量关系.html`）/ `能耗管理-表具明细` / `数据质量自检`  
消费：`总览看板`（默认实时；实时态仅 PUE/CLF/PLF/OLF + 主瞬时量近 24 小时时序，无顶部时间控件/成本/导出；切历史恢复原三卡与导出；含「能耗趋势与流向」type：趋势分析 / 流向分析桑基；原独立 `能耗流向分析` 侧栏已并入）、`用能对比分析`、`用能定额考核`、`节能分析`  

指标两层（需求 5）：**物模型层（类型层）**在《物模型-模型详情》「指标」Tab——只列「这类对象有哪些指标」，一行一个具体指标（含继承来的，页面不区分来源），产品预置、现场只读；**实例层**在《能效视图》父级对象详情的「指标列表」——按本体、下级组件分组，并按各实例的数据现状出值、判定「可算/待配置」。平台不提供组件详情页；组件指标的曲线、计算口径与下钻保留组件实例 ID。C1 与 B1–B5 实时值按默认调度计算后写入时序存储，支持当前值、历史曲线及重算状态；拓扑变化自动重算全部保留期历史并原子替换。模型详情不出值、不显示状态、无口径弹窗。

口径：能源拓扑分为能耗计量与供电两类关系。独立页《能源拓扑配置》承担跨对象批量作业：对象树支持设备实例及下级组件实例作为计量对象，两者统一使用实例 ID，实例层级不自动生成计量关系；中间列表显式展示关系类型，仅能耗计量关系设置输入/输出方向；表具池只标识可配置的拓扑关系类型，关联状态由“未关联 / 已关联”筛选表达，同一支路可同时配置两类关系。今日试算与 Excel 全局导入导出仅使用能耗计量关系；单对象配置在《能效视图》「能效配置」Tab，与独立页维护同一份关系数据。表具明细仍是用量 / 成本唯一事实归档；实时指标时序是可重算派生结果。注意实施期自检与运行期数据质量校验是两套规则。

### 3.4 模型训练与仿真 `flow.model_train_sim`

`机理模型训练` → `创建-编辑模型` | `手动训练` | `模型评估详情`  
`系统仿真建模` → `创建仿真模型` → `仿真校验`

### 3.5 机房寻优与诊断 `flow.room_opt_diagnosis`

`机房总览` → 柔性 / 加减机；健康度 → `机房智能诊断`  
`机房寻优-机房热力模型列表` → `创建机房模型` → `步骤2`（通道模式影响诊断是否可用）

#### v2.6 设备能力架构（req.v26.room_opt_device_capability）

四层生产架构，**不新增页面入口**，增强既有加减机 / 柔性两页：

| 层 | 职责 |
|----|------|
| L1 | 空气侧环境感知 |
| L2 | 设备能力插件：PowerModel / CoolingModel / OptimizationCapability |
| L3 | 加减机 / 柔性决策（手动与自动共用，不复制两套算法） |
| L4 | 调度承载：持久化建议与人工处理状态，生成执行上下文 |

要点：

- 覆盖水冷、风冷 DX、水冷+风冷混合编队；规则配置为居中弹窗 + 按对象类型生成规则表；投入/投出沿用动态对象类型分组。
- PowerModel 仅加减机消费基础参考功率；CoolingModel 分离 `Q_constraint` 与 `Q_est`，风冷安全冷量按室外高温收紧且 `Q_est` 不重复折减。
- 柔性以 `Q_feedback_safe = min(ΣQ_est,safe, ΣQ_constraint,running)` 对照 `K_safe` 门控；节能模式按 `R_target = K_safe + δ_target` 目标带产生带上省电、带内保持和带下预防性增冷，并继续 A~E 保护、质量折减、V1/V2 串行与超温紧急分支。
- **执行闭环**：当前值只信本轮真实控制点回读；L3每轮根据真实控制值和真实物理反馈重新计算，不设置固定等待；手动 / 自动、写入结果与回读确认复用既有寻优记录、下发策略和设备级下发记录；L4负责真实回读确认及相同待生效目标去重，不暂停L3。
- **保障模式**：按房间缺口分配最小步动作预算；含保障动作死区、最高冷通道温升触发、连续2个可调轮次退出；不引入 PID / 积分 / 残差累计 / 动作损耗目标。
- 验收：18 热区 RC plant + 下发记录协调器；单元测试 100/100、记录闭环金标 9/9、Gate 1～3 共 11/11 通过，Gate 4A 当前 13/15；两个规划响应参数场景末 12 轮稳定性未达标，仍为 `GATE_4A_FINDINGS`。现场单变量阶跃校准并复跑前不进入 Gate 4B，也不开放现场自动下发。定频启停柔性、L1 高阶接入、定频窗口聚合、柔性响应在线自适应分别见需求池 #24 / #23 / #25 / #26。
- PRD：`04-需求文档/AI节能v2.6 版本/末端寻优设备能力抽象架构设计方案.html`

#### v2.6 可解释性（req.v26.room_opt_explainability）

以统一 L3 结果与实际计算轨迹为唯一事实源，**不改变** `zi / V1* / V2*`：

- 加减机：记录真实决策轨迹（全局组合 / 基准修正 / 邻域动作），不做候选排名伪因果；`P_reference` 仅作方案比选用参考功率，不作实际节能量。
- 柔性：直接消费 L3 `mode` 解释保障锁定与连续退出确认；按目标带覆盖带上省电、带下预防性增冷、热点增冷、带内 / 趋势 / 热点在途保持、紧急增冷、凝露部分恢复及 V2-only 建议。
- UI 落点：两页「本轮寻优结论条」；设备原因仅在平面图浮窗与手动下发弹窗展示同一段“本轮建议”文案（无 chip 列、无独立解释弹窗）。实际下发、回执和回读继续由 L4 既有记录承载。
- PRD：`04-需求文档/AI节能v2.6 版本/末端寻优结果可解释性需求方案.md`

#### v2.6 风冷精密空调功率（req.v26.pac_power_predict）

算法侧、无新原型页：复用 `AirCooledPAC`；L0 零数据可出值，L1 仅数码涡旋/变频；定频启停本期仅 L0。寻优消费由设备能力架构承接；物模型 / L0·L1 见 `04-需求文档/AI节能v2.6 版本/风冷精密空调功率预测模型需求文档.md`。

### 3.6 安全与下发配置 `flow.config_crud_safety_dispatch`

`安全边界-列表` → 创建/编辑  
`智控下发策略管理` → `创建-编辑智控下发策略`  
`安全回退策略` → `创建-编辑安全回退策略`

---

## 4. 已知坑（noise）

| ID | 说明 |
|----|------|
| `noise.version_plan_stale` | `01-版本计划.md` 不可作当前状态源 |
| `noise.dual_data_quality` | 同页自检：v2.1 实施期 vs v2.5 运行期表具数据质量校验 |
| `noise.manual_train_orphan` | `手动训练.html` 未进 index 拓扑 |
| `noise.v24_explore_unlist_html` | 清单多工况条目未直链 4 个 HTML |
| `noise.v26_no_new_page` | v2.6 三项均无新原型页：功率预测为 algo_only；设备能力与可解释性增强既有加减机 / 柔性两页 |

完整列表见 `product-graph.yaml` → `noise`。

---

## 5. 按任务打开什么

| 任务 | 先看 |
|------|------|
| 改多工况页 | flow.multi_condition_explore + v2.4 多工况 PRD 文首 |
| 改总览/流向/定额 | flow.energy_config_to_consume + v2.5 两册 PRD |
| 改冷站寻优/置信 | flow.cold_station_opt_family + v2.3 泛化性文档清单表 |
| 改冷站诊断/规则 | flow.cold_station_diagnosis + 现网 `#/diagnosis-overview` / `#/diagnosis-rules` |
| 改机房加减机/柔性 | flow.room_opt_diagnosis §3.5 + req.v26.room_opt_device_capability / explainability |
| 改机房诊断 | flow.room_opt_diagnosis + v1.9 方案 §一–§二 |
| 改风冷精密空调功率 | req.v26.pac_power_predict（algo_only，无新页）+ 设备能力承接消费 |
| 改模型训练/仿真 | flow.model_train_sim + v2.3/v2.2 相关条目 |

读序总览：`00-产品全貌.md` §4。
